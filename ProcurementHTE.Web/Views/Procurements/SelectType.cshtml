@model IEnumerable<ProcurementHTE.Core.Models.JobTypes>
@{
    ViewData["Title"] = "Select Job Type";
}


<header class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h3 mb-0">Select Job Type</h1>
    <a asp-action="Index" class="btn btn-outline-secondary" role="button"><i class="bi bi-arrow-left"></i> Back to List</a>
</header>
@if (!Model.Any())
{
    <div class="alert alert-warning">Job type not available.</div>
}
else
{
    <div class="row row-cols-1 row-cols-md-3 g-4">
        @foreach (var type in Model)
        {
            <div class="col">
                <div class="card h-100 shadow-sm border-0 hover-shadow transition"
                     style="cursor:pointer;"
                     onclick="selectType('@type.JobTypeId')">
                    <div class="card-body d-flex flex-column justify-content-between">
                        <div>
                            <h5 class="card-title text-primary fw-semibold">@type.TypeName</h5>
                            @if (!string.IsNullOrWhiteSpace(type.Description))
                            {
                                <p class="card-text text-muted small">@type.Description</p>
                            }
                            else
                            {
                                <p class="card-text text-muted small">No description available.</p>
                            }
                        </div>
                        <div class="mt-3">
                            <button type="button"
                                    class="btn btn-outline-primary w-100">
                                Select
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>

    <form id="selectTypeForm" asp-action="SelectType" method="post" class="d-none">
        <input type="hidden" name="jobTypeId" id="jobTypeId" />
        @Html.AntiForgeryToken()
    </form>
}

@section Scripts {
<script>
    function selectType(id) {
        document.getElementById('jobTypeId').value = id;
        document.getElementById('selectTypeForm').submit();
    }
</script>
}
