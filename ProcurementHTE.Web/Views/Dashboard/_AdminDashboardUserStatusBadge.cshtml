@model UserActivityViewModel

@if (Model.IsOnline)
{
    <span class="badge bg-success user-status-badge"><i class="bi bi-circle-fill admin-dot"></i> Online</span>
}
else
{
    <span class="badge bg-secondary user-status-badge"><i class="bi bi-circle admin-dot"></i> Offline</span>
}
